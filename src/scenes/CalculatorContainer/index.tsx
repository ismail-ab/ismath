import { Button } from "@material-ui/core";
import * as React from "react";

const CalculatorContainer: React.SFC = () => (
  <div>
    <Button variant="raised" color="primary">
      Calculate
    </Button>
  </div>
);

export default CalculatorContainer;
